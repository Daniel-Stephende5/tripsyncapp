const styles = {
  container: 'w-full min-h-screen px-6 py-10 bg-white',
  title: 'text-3xl font-bold mb-4',
  datepickerWrapper: 'mb-6 relative z-50 max-w-xs',
  
  mapWrapper: 'w-full h-[500px] mb-6 z-0', // Use this instead of inline styling
  buttonGroup: 'flex gap-4 mt-4',
  primaryButton: 'bg-blue-600 text-white px-6 py-2 rounded hover:bg-blue-700 transition',
  secondaryButton: 'bg-gray-100 border border-gray-300 text-gray-700 px-6 py-2 rounded hover:bg-gray-200 transition',
};

export default styles;
